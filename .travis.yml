language: python
python:
  - "3.3"
  - "3.4"
# command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
#
virtualenv:
  system_site_packages: true

# Setup anaconda
before_install:
  - ls -la
  - sudo apt-get install hdf5-tools
  - sudo apt-get install -qq python-numpy python-scipy
  - git clone https://github.com/rogerbinns/apsw.git && cd apsw
  - python setup.py fetch --missing-checksum-ok --all build --enable-all-extensions  install --user && cd ..
  # upgrade to new pip with wheels
  - pip install pip --upgrade
  # pytables complains if this isn't installed beforehand
  - pip install -r requirements.txt
# Install packages
install:
  - python setup.py install
  - export PYTHONPATH=/home/travis/miniconda/lib/python3./site-packages
# command to run tests, e.g. python setup.py test
script:  
  - py.test -v tests/BaseTest.py
